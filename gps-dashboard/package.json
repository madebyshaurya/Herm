{
  "name": "herm-pi-runtime",
  "version": "3.0.0",
  "description": "Herm Raspberry Pi runtime — hardware discovery, module management, GPS, camera service, and Herm cloud sync.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check hardware.js && node --check modules.js && node --check sim7600.js && node --check settings.js"
  },
  "dependencies": {
    "@serialport/parser-readline": "^12.0.0",
    "serialport": "^12.0.0"
  }
}
